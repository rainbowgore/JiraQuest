# Core dependencies
pandas>=1.5.0          # For data manipulation and handling Excel/CSV files
requests>=2.28.0       # For making HTTP requests to Jira and Slack APIs
python-dotenv>=1.0.0   # For managing environment variables

# Optional dependencies
tableau-api-lib>=0.1.0 # For Tableau integration (if applicable)

# Development dependencies (optional, for contributors)
pytest>=7.2.0          # For testing
black>=23.1.0          # For code formatting
mypy>=1.0.0            # For type checking